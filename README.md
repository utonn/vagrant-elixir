elixir環境が作れます。

vagrant上にansibleを利用して構築します。
elixir,erlangはともにソースからインストールしてます。

## Requirements

- vagrant

## How To Use

``` sh
vagrant up
```

## Spec

- Centos/7
- erlang (latest)
- elixir (latest)
